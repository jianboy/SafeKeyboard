ext {
    build = [
            minSdkVersion   : 16,
            targetSdkVersion: 29,
            compileSdkVersion:29,
            versionCode     :51,
            versionName     : "1.1.0",
    ]
    lib = [

    ]
    sign = [
            keyAlias     : "签名别名",
            keyPassword  : "签名密码",
            storeFile    : "签名文件路径",
            storePassword: "签名文件密码",
    ]
}